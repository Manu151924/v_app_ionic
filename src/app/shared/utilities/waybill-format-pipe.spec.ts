import { WaybillFormatPipe } from './waybill-format-pipe';

describe('WaybillFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new WaybillFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
